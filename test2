Fix Angular routing so that `/dashboard-v2` loads the NEW dashboard UI from `src/app-v2`
and NOT the legacy UI from `src/app`.

Context:
- Legacy Angular app lives in `src/app`
- New Angular app lives in `src/app-v2`
- `/dashboard-v2` currently renders legacy UI (incorrect)
- New UI exists and compiles correctly

Your tasks (MANDATORY):

1. Trace Angular route resolution step-by-step and identify EXACTLY:
   - Which route currently matches `/dashboard-v2`
   - Why legacy UI is rendered
   - File name + line number causing the issue

2. Apply the MINIMAL FIX required so that:
   `/dashboard-v2`
     → lazy loads `AppV2Module`
     → loads `DashboardModule`
     → renders `DashboardPageComponent`

3. You are allowed to modify ONLY:
   - `src/app/app-routing.module.ts`
   - `src/app-v2/app-v2-routing.module.ts`
   - `src/app-v2/dashboard/dashboard-routing.module.ts`

4. Enforce these rules:
   - `/dashboard-v2` route MUST appear before any `''` or `**` routes
   - No wildcard or default route may intercept `/dashboard-v2`
   - Legacy routes must continue to work unchanged

5. Add a TEMPORARY verification marker:
   In `DashboardPageComponent.html`, add:
   `<h1 style="color:red">DASHBOARD V2 ACTIVE</h1>`

6. Ensure that visiting:
   `http://localhost:4200/dashboard-v2`
   shows the marker above.

Output format (STRICT):

1) Root cause (file + line)
2) Exact code changes made
3) Final `app-routing.module.ts`
4) Final `app-v2-routing.module.ts`
5) Final `dashboard-routing.module.ts`
6) Verification steps

Rules:
- Do NOT touch backend
- Do NOT refactor components
- Do NOT speculate
- Do NOT explain Angular concepts
- Must result in `/dashboard-v2` loading new UI

Success = red marker visible on `/dashboard-v2`