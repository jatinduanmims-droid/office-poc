Fix all Angular standalone vs NgModule conflicts in dashboard-v2.

STRICT RULES:
- dashboard-v2 must be 100% NgModule-based
- No standalone components allowed
- No imports inside @Component
- All imports must live in DashboardV2Module only

TASKS:
1. Remove `standalone: true` from ALL dashboard-v2 components
2. Remove `imports: []` from ALL dashboard-v2 components
3. Ensure ALL dashboard-v2 components are declared in DashboardV2Module
4. Ensure NO dashboard-v2 component is declared in any other module
5. Move ALL Angular Material + Charts imports into DashboardV2Module
6. Fix routing so `/dashboard-v2` loads DashboardPageComponent only
7. Remove any conflicting direct routes or legacy imports

OUTPUT:
- Zero NG3004 errors
- Zero standalone-related errors
- `/dashboard-v2` loads cleanly
- Legacy dashboard untouched

Do not explain.
Fix completely.