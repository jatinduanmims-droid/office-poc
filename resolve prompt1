Fix Angular errors related to standalone components being declared in an NgModule.

Context:
- Angular v14+
- Components are marked with `standalone: true`
- Errors occur because they are incorrectly listed in `declarations`

Files involved:
- src/app-v2/dashboard/dashboard.module.ts
- src/app-v2/dashboard/components/kpi-cards/kpi-cards.component.ts
- src/app-v2/dashboard/components/trend-charts/trend-charts.component.ts
- src/app-v2/dashboard/components/data-table/data-table.component.ts
- src/app-v2/dashboard/dashboard-page.component.ts

MANDATORY RULES:
- Do NOT remove `standalone: true` from any component
- Do NOT touch routing
- Do NOT touch backend
- Do NOT refactor logic or HTML
- Fix ONLY module wiring

Steps to perform:

1. Open `dashboard.module.ts`
2. REMOVE all standalone components from `declarations`
3. ADD those components to the `imports` array instead
4. Ensure `CommonModule` is also present in `imports`
5. Verify `DashboardPageComponent`:
   - If standalone → import child components in its `imports`
   - If NOT standalone → it remains the only declared component

Expected final structure:

@NgModule({
  declarations: [
    DashboardPageComponent
  ],
  imports: [
    CommonModule,
    KpiCardsComponent,
    TrendChartsComponent,
    DataTableComponent
  ]
})

Return:
- Updated `dashboard.module.ts`
- Confirmation that errors NG6008 are resolved

DO NOT explain Angular theory.
DO NOT suggest alternative approaches.
JUST FIX IT.